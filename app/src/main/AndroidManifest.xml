<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mehdi.myapplication">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:usesCleartextTraffic="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NoBar"
        tools:targetApi="31">
        <activity android:name=".MainActivity" android:exported="true" android:windowSoftInputMode="stateVisible">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ConnectionActivity" />
        <activity android:name=".SearchInChatActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".GroupHomeActivity" />
        <activity android:name=".GroupActivity" />
        <activity android:name=".SearchGroupActivity" />
        <activity android:name=".SearchInSiteActivity" />
        <activity android:name=".PostActivity" />
        <activity android:name=".DiscussionActivity" />
        <activity android:name=".AccountParametersActivity" />
        <activity android:name=".CreatePostActivity" />
        <activity android:name=".AnswerActivity" />
        <activity android:name=".CreateProjectActivity" />
        <activity android:name=".CreateGroupActivity" />
        <activity android:name=".SubscriptionActivity" />
        <activity android:name=".SearchPostResultActivity" />
        <activity android:name=".SearchUserResultActivity" />


    </application>

</manifest>